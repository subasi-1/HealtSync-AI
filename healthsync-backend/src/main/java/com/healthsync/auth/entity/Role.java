package com.healthsync.auth.entity;

/**
 * Enumeration representing user authorization roles in the system.
 */
public enum Role {
    SUPER_ADMIN,
    DISTRICT_ADMIN,
    HOSPITAL_ADMIN,
    DOCTOR,
    PATIENT,
    STAFF,
    USER
}
